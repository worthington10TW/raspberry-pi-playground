name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Install dependecies
        uses: VaultVulp/action-pipenv@v2.0.1
        with:
          command: install -d
    - name: Test
      run: |
        make test
    - name: Publish
      run: |
        make publish
    - uses: actions/upload-artifact@v2
      with:
        name: pi-build-monitor
        path: dist/pi-build-monitor-0.1.0.tar.gz